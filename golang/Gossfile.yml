command:
  dep version:
    exit-status: 0
    stdout:
      - "dep:"
      - " version     : v0.4.1"

  go version:
    exit-status: 0
    stdout:
      - go version go1.10.1 linux/amd64

  which dep:
    exit-status: 0
    stdout:
      - /go/bin/dep